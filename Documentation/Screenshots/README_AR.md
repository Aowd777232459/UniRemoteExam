# لقطات الإصدار v6 المطلوبة

التقط بعد التشغيل الفعلي الصور التالية: تسجيل الدخول، إدارة المقررات، إنشاء الاختبار، نموذج الإجابة، مراجعة المدير، شاشة الطالب والمؤقت، التصحيح المقالي، النتيجة، مراجعة الإجابات، التقرير وسجل المراقبة.

الصور الموجودة في `Legacy_Screenshots_v5` مرجعية للنسخة السابقة ولا تُستخدم كإثبات لاختبار v6.
